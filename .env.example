# ApertureData Configuration
# Option 1: Use key for cloud instance (recommended)
# APERTUREDB_KEY=your_connection_key_here

# Option 2: Use URL
# APERTUREDB_URL=https://scheduler-agent-3i2pmg9w.farm0004.cloud.aperturedata.io

# Option 3: Use separate host/port (for local instances)
# APERTUREDB_HOST=localhost
# APERTUREDB_PORT=55555

# Authentication (choose one if not using key)
# APERTUREDB_TOKEN=your_token_here
# APERTUREDB_USERNAME=your_username
# APERTUREDB_PASSWORD=your_password

# SSL Configuration (usually true for cloud instances)
APERTUREDB_USE_SSL=true
