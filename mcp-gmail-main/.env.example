# Gmail & Calendar MCP Server Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# Port for the MCP server
MCP_GMAIL_PORT=8090

# Host address
# Use 0.0.0.0 to accept connections from anywhere
# Use 127.0.0.1 for local-only access
MCP_GMAIL_HOST=127.0.0.1

# =============================================================================
# GOOGLE OAUTH CONFIGURATION
# =============================================================================
# Path to your OAuth credentials JSON file from Google Cloud Console
# Download this from: https://console.cloud.google.com/apis/credentials
MCP_GMAIL_CREDENTIALS_PATH=credentials.json

# Path where the OAuth token will be stored after authentication
MCP_GMAIL_TOKEN_PATH=token.json

# Maximum number of results for search queries
MCP_GMAIL_MAX_RESULTS=10

# =============================================================================
# NGROK DEPLOYMENT & AUTHENTICATION
# =============================================================================
# Bearer token for API authentication when deploying via ngrok
# This will be auto-generated by deploy_ngrok.py and saved to .bearer_token
# You can manually set it here if needed
# MCP_BEARER_TOKEN=your-secure-bearer-token-here

# =============================================================================
# TELNYX INTEGRATION
# =============================================================================
# When configuring in Telnyx AI Agent:
# 1. Name: Gmail Calendar MCP
# 2. Type: SSE
# 3. URL: https://your-ngrok-url.ngrok-free.app/sse
# 4. API Key: Use the bearer token from .bearer_token file or MCP_BEARER_TOKEN above
